/* 
                   refVar ---> DOM ELEMENT
                   State Variables the User Interface
                   isPlaying = false audio is not playing
                   
                   stateVariable 
                   developers way of assigning a value 
                   to represent the state of UI environment..
                   
                   it allows us to set if a refVar meets a condition. True is one thing. False is another
                   Uses if and else

*/